import React, { useState, useMemo } from 'react';
import { IndexFiltersModeContext } from './context.js';
import { IndexFiltersMode } from './types.js';

function IndexFiltersManager({
  children
}) {
  const [mode, setMode] = useState(IndexFiltersMode.Default);
  const value = useMemo(() => ({
    mode,
    setMode
  }), [mode, setMode]);
  return /*#__PURE__*/React.createElement(IndexFiltersModeContext.Provider, {
    value: value
  }, children);
}

export { IndexFiltersManager };
